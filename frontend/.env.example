# Leave blank to use Vite's dev proxy (see vite.config.js) -> http://localhost:5000
# Set this only if the backend is hosted somewhere other than the dev proxy,
# e.g. VITE_API_BASE_URL=https://api.recipeatlas.example.com
VITE_API_BASE_URL=
